{
  "name": "app",
  "version": "1.0.0",
  "description": "basic app backend service",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rawada",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
